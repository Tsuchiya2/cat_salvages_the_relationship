h1.mt-5.text-break
  = @alarm_content.body
h3.mt-5
  | アラームカテゴリー：
  = @alarm_content.category_i18n
- if current_operator.operator?
  .mt-5
    = link_to '🐾 編集 🐾', edit_operator_alarm_content_path(@alarm_content), class: 'btn btn-secondary rounded-pill font-monospace'
  .my-5
    = button_to '- 削除 -', operator_alarm_content_path(@alarm_content), method: :delete, class: 'btn btn-danger rounded-pill font-monospace', form: { style: 'display: inline;' }
