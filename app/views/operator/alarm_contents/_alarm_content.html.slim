tr
  td
    = alarm_content.id
  td
    - if current_user.operator?
      = link_to alarm_content.body.truncate(10), operator_alarm_content_path(alarm_content), class: 'link-dark text-decoration-none'
    - else
      = alarm_content.body.truncate(10)
  td
    = alarm_content.category_i18n
