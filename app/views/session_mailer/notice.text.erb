【Warning】ロック状態のアカウントにアクセスがありました
===========================

以下のアカウントがロック状態にも関わらず、ログインを試みた模様です。確認と対策をお願いします。

アカウント名：<%= @operator.name %>

ロック解除時間：<%= l(@operator.lock_expires_at, format: :long) %>

アクセス先のIP：<%= @access_ip %>
